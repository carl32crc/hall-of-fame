import styled from 'styled-components'

import { COLORS } from 'global-styles/colors'

export const StyledDivider = styled.hr`
  border: 3px solid ${COLORS.HALL_OF_FAME_GREEN};
  width: 150px;
  margin: 0;
`
