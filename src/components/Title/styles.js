import styled from 'styled-components'

import { fontFamily } from 'global-styles/fontFamily'
import { COLORS } from 'global-styles/colors'

export const StyledTitle = styled.h1`
  color: ${COLORS.HALL_OF_FAME_BLACK};
  margin: 0;
  ${fontFamily}
`
